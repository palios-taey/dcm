__pycache__/
*.pyc
.venv/
.claude/
# pip / build artifacts (created by `pip install .`)
build/
dist/
*.egg-info/
# Council runtime inputs/outputs are OPERATOR CONTENT — runtime args, never committed
# (council_cli.py: "Inputs are FILES (operator content is a runtime arg, never committed)").
problem*.txt
rules*.txt
council_out*.txt
*.council.out

# Retired path components are history-only, not current public surface.
archive/
archived/
deprecated/
superseded/
obsolete/
legacy/
