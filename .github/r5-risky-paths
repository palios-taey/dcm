# R5 risky surfaces for DCM — a PR touching ANY of these requires both adversarial
# auditors (audit/grok + audit/gatekeeper) to post a success commit status
# before it can merge. The r5-audit-gate workflow enforces this mechanically.

# Substrate, council, scaling, adapter, and test modules:
*.py

# Shell entrypoints and CLIs:
setup.sh

# Agent and transport contracts:
docs/**/*.md
docs/*.md
README.md
SKILL.md
AGENTS.md
CLAUDE.md

# CI/gate config itself (self-protecting — a PR weakening the gate is gated):
.github/workflows/*.yml
.github/r5-risky-paths
scripts/r5-risk-classifier

# Packaging and dependency specifications:
pyproject.toml
requirements.txt
